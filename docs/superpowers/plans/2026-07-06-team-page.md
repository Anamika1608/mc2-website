# Team Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the MC²+ **Team** (`/team`) page as an exact replica of the Figma design (file `u4NLOVKuXUTKULhSn0sAJQ`, frame `1:11940` "Desktop", 1920×4840), responsive below desktop per our established rules.

**Architecture:** Five section components under `src/components/team/` composed by `src/pages/team.astro` (replacing the current `<StubPage>`). Assets (9 grayscale person photos, curves, spark, chevron) exported from Figma once in Task 1, plus one new color token. Reuses the existing `PillButton.astro` (already on `main`) and the untouched site chrome (Header/Footer/BaseLayout).

**Tech Stack:** Astro 7.0.6, Bun, scoped Astro styles + existing tokens, Figma MCP (`mcp__plugin_figma_figma__*`) as the design source. No test runner — verification is `bunx astro check` + `bun run build` + `grep` copy sweeps + a Figma screenshot gate (same as About).

## The design source (read this first)

Illustrator import: headings are real text nodes (exact styles via `get_design_context`); body/description/role-label/subheading/lede text is outlined into vectors (copy given verbatim per task below; sizes derived from bounding boxes); person photos are raster fills (export by node ID). No Figma variables.

**Figma MCP access for every implementer:** load tools via ToolSearch `select:mcp__plugin_figma_figma__get_design_context,mcp__plugin_figma_figma__get_screenshot,mcp__plugin_figma_figma__get_metadata,mcp__plugin_figma_figma__download_assets`. File key `u4NLOVKuXUTKULhSn0sAJQ`. Curl asset URLs to disk immediately (they expire). If Figma tools are unavailable, report BLOCKED.

**Section → node map** (content in frame `1:11941`):

| Section | Key nodes |
|---|---|
| Hero | eyebrow `1:13353`, H1 group `1:13356` (lines `1:13357/1:13358/1:13359`), lede `1:13360`, curves `1:13475` |
| Board of Directors | header `1:12887` (eyebrow `1:12888`, H2 `1:12889`), cards `1:13078` (Neeraj `1:13079`, Vikas `1:13131`, Sanjay `1:13171`, Akshay `1:13283`, Sandeep `1:13211`; photos `1:13127/1:13167/1:13207/1:13343/1:13279`) |
| Advisory Board | header `1:12977` (eyebrow `1:12978`, H2 `1:12979`), cards `1:12592` (Abhay `1:12593`, Madhukar `1:12679`, Neelkanth `1:12751`, Pawan `1:12826`) |
| Operating Team | eyebrow `1:13349`, H2 `1:13350`, cards group `1:11944` |
| CTA | group `1:12481` (H2 `1:12483`, paragraph `1:12482`) |
| Header/Footer (built) | `1:13481` — do not rebuild |

## Verified type scale (from `get_design_context`)
- Hero H1: Faculty **80px**, `#1e1e3e`; accent words ("speed", "accountability") `#ff7400`, inline spans.
- Eyebrows: Faculty **27.07px**, **`#ff7400`**, letter-spacing ≈6.77px.
- Section H2s: Faculty **65px**, `#1e1e3c`.
- Person name: Faculty **≈30.1px**, `#1e1e3e`.
- Role/dept label: Faculty **≈14.88px**, `#949494`, uppercase, letter-spacing ≈1.49px.
- Subheadings/descriptions/lede: Sora, sizes derived from bbox per section.

## Global Constraints
- Working directory `/Users/anamika/work/mc2-website`; branch `feat/team-page` (already cut from `main`).
- **Bun only** (`bun run dev|build`, `bunx astro check`) — never npm/npx/yarn/pnpm.
- Git commits: **plain messages only**. **Never** add `Co-Authored-By: Claude ...` or "Generated with Claude Code" trailers.
- Colors via tokens: `#1e1e3c`→`--color-ink-alt`, `#1e1e3a`→`--color-ink`, `#f37d2c`→`--color-orange`, `#ff7400`→**`--color-orange-bright`** (added in Task 1), `#959595`→`--color-gray`, `#f3f2eb`→`--color-cream`, `#e8e6da`→`--color-line`. Role-label `#949494` may use `--color-gray` (imperceptible delta) or a documented literal. Any other tokenless fill = documented literal. Never eyeball — read fills from `get_design_context`.
- Do NOT modify `Header.astro`, `Footer.astro`, `Logo.astro`, `BaseLayout.astro`, `nav.ts`, or `PillButton.astro`. The ONLY shared-file edit is adding one token to `global.css` in Task 1.
- Breakpoints: reuse `1023.98px` desktop cut; each task defines `<1024`/`<768`(or `<560`) stacking (design is desktop-only; responsive is ours).
- Hover states minimal/consistent with chrome. Orange-on-cream accent contrast is a KNOWN deferred item — replicate design colors, don't "fix".
- Copy **byte-for-byte** as drawn. Person photos are grayscale in the design — if an export comes back in color, apply `filter: grayscale(1)` and report.
- Verify-don't-assume: if a fetched style contradicts this plan, Figma wins — note the correction in your report.

## Deviations to document in-code
1. Responsive behavior + hover states — ours (design is desktop-only).
2. Orange-on-cream accent text (`#ff7400`/`#f37d2c` on cream) — KNOWN deferred contrast item; replicate, don't fix.
3. Copy byte-for-byte.

## File Structure After This Plan
```
src/
├── assets/team/                         # Task 1 exports (names are the contract)
├── components/team/
│   ├── TeamHero.astro                   # Task 2
│   ├── BoardOfDirectors.astro           # Task 3
│   ├── AdvisoryBoard.astro              # Task 4
│   ├── OperatingTeam.astro              # Task 5
│   └── TeamCta.astro                    # Task 6
├── pages/team.astro                     # Task 7 (replaces StubPage)
└── styles/global.css                    # Task 1 — adds --color-orange-bright
```

---

### Task 1: Figma asset export + new token

**Files:**
- Create: `src/assets/team/*` (contract below)
- Modify: `src/styles/global.css` (one token)

**Interfaces:**
- Produces (name contract later tasks rely on):
  - `src/assets/team/hero-curves.svg` (orbital art `1:13475`; split L/R if separate — report)
  - `src/assets/team/spark-orange.svg` (4-point star; expect fill `#ff7400` — report actual)
  - `src/assets/team/scroll-arrow.svg` (down chevron)
  - Director photos: `dir-neeraj.png`, `dir-vikas.png`, `dir-sanjay.png`, `dir-akshay.png`, `dir-sandeep.png`
  - Advisor photos: `adv-abhay.png`, `adv-madhukar.png`, `adv-neelkanth.png`, `adv-pawan.png`
  - Token `--color-orange-bright: #ff7400;` in `:root`.

- [ ] **Step 1: Confirm branch + clean tree**

Run: `git branch --show-current && git status --porcelain`
Expected: `feat/team-page`, empty status. Else BLOCKED.

- [ ] **Step 2: Add the token**

In `src/styles/global.css`, in `:root` immediately after the `--color-orange-soft` line, add:
```css
  --color-orange-bright: #ff7400; /* team eyebrows, H1 accents, sparks — Figma 1:12888 */
```

- [ ] **Step 3: Locate + export assets**

Load Figma tools. For each person, run `download_assets` on the card/photo node to get the raster source image (fileKey `u4NLOVKuXUTKULhSn0sAJQ`); `curl` to the target path immediately. Photo → person mapping by card position (verify against a screenshot of each card — do NOT rely on the `Links/` folder):
- Directors row 1: Neeraj (`1:13079`/photo `1:13127`), Vikas (`1:13131`/`1:13167`), Sanjay (`1:13171`/`1:13207`); row 2: Akshay (`1:13283`/`1:13343`), Sandeep (`1:13211`/`1:13279`).
- Advisors: Abhay (`1:12593`), Madhukar (`1:12679`), Neelkanth (`1:12751`), Pawan (`1:12826`).
Export vectors (`hero-curves`, `spark-orange`, `scroll-arrow`) as SVG (`defaultFormat: "svg"`) from their nodes (curves `1:13475`; find the spark + chevron nodes under the hero via `get_metadata`). Photos as PNG (`defaultFormat: "png"`, `defaultScale: 2`).

- [ ] **Step 4: Verify each asset**

View every file (Read renders images). Confirm each photo is the correct person (grayscale) and each SVG is non-empty with no opaque full-canvas background. Report the spark's actual fill color. Re-export anything wrong.

- [ ] **Step 5: Verify build**

Run: `bunx astro check 2>&1 | tail -4` → 0 errors, 0 warnings (token change compiles).

- [ ] **Step 6: Commit**
```bash
git add src/assets/team src/styles/global.css
git commit -m "feat: export team page assets and add bright-orange token"
```

---

### Task 2: TeamHero section

**Files:** Create `src/components/team/TeamHero.astro`

**Interfaces:** Consumes tokens, `.container`, assets `hero-curves.svg`, `spark-orange.svg`, `scroll-arrow.svg`. Produces `TeamHero.astro` (no props).

- [ ] **Step 1: Fetch styles/geometry** — `get_design_context` on `1:13353` (eyebrow), `1:13357` (H1), `1:13360` (lede); `get_screenshot` on curves `1:13475` (maxDimension 1536) for curve+spark placement. Binding facts (Figma wins):
  - Eyebrow `TEAM & GOVERNANCE`: Faculty 27px `--color-orange-bright`, tracked ~0.25em, centered; hairline underline (confirm).
  - H1 (Faculty 80px `#1e1e3e` — use `--color-ink-alt` if it matches, else literal `#1e1e3e`; confirm): `Leadership` / `built for <accent>speed</accent>` / `and <accent>accountability</accent>.` accent `--color-orange-bright`. Line-height from node y-deltas.
  - Lede (Sora, centered, size from `1:13360` bbox): `A board that carries national gravitas, an advisory bench deep in energy and deep-tech, and an operating team that moves like a startup.`

- [ ] **Step 2: Build** mirroring `src/components/about/AboutHero.astro` (READ it first — same `.hero > .orbit + .hero-inner` skeleton, curve/spark absolute positioning, `overflow:hidden`). Copy verbatim; H1 accent spans in `--color-orange-bright`. Note `#1e1e3e` is NOT an existing token — if it doesn't equal `--color-ink-alt` (`#1e1e3c`), use a documented literal `#1e1e3e` and say so. If `hero-curves.svg` bakes in its sparks, drop the `<SparkOrange>` overlays (as AboutHero did) — verify.

Responsive: <1024 H1 `clamp(2.4rem, 7vw, 5rem)`, lede max-width 90%, curves scale/crop.

- [ ] **Step 3: Verify** — `bunx astro check 2>&1 | tail -5` → 0/0.
- [ ] **Step 4: Commit** — `git add src/components/team/TeamHero.astro && git commit -m "feat: add team hero section"`

---

### Task 3: BoardOfDirectors section

**Files:** Create `src/components/team/BoardOfDirectors.astro`

**Interfaces:** Consumes tokens, `.container`, `astro:assets` `<Image>` (5 director photos), `spark-orange.svg`. Produces `BoardOfDirectors.astro` (no props).

- [ ] **Step 1: Fetch context** — `get_design_context` + `get_screenshot` on `1:12591`/`1:13078` (maxDimension 2048). Binding facts: eyebrow `GOVERNANCE` (Faculty 27px `--color-orange-bright`); H2 `Board of Directors` (Faculty 65px `--color-ink-alt`); subheading (Sora, muted, centered, size from bbox); 5 cards photo-left (square grayscale, cream/hairline border) + text-right, laid out **3 in row 1, 2 in row 2**; `--color-orange-bright` spark at each photo's top-left. Card text: name Faculty ≈30px `#1e1e3e`; role label Faculty ≈15px `#949494` uppercase tracked; description Sora.

- [ ] **Step 2: Build.** Import the 5 photos; data array + map. Copy verbatim (name / role / description):
  1. `Dr. Neeraj Mittal` / `CHAIRMAN & DIRECTOR` / `Secretary, Ministry of Petroleum and Natural Gas`
  2. `Shri Vikas Kaushal` / `DIRECTOR` / `Chairman & Managing Director, HPCL`
  3. `Shri Sanjay Khanna` / `DIRECTOR` / `Chairman & Managing Director, BPCL`
  4. `Shri Akshay Kumar Singh` / `DIRECTOR` / `Managing Director and Chief Executive Officer, Petronet LNG Ltd.`
  5. `Shri Sandeep Maheshwari` / `DIRECTOR & CHIEF EXECUTIVE OFFICER` / `Executive Director, Corporate Strategy & Business Development, HPCL`
  Structure per card:
```astro
<article class="card">
  <div class="photo-wrap">
    <SparkOrange class="card-spark" aria-hidden="true" />
    <Image src={p.photo} alt={p.name} widths={[160, 320]} sizes="160px" class="photo" />
  </div>
  <div class="meta">
    <h3>{p.name}</h3>
    <p class="role">{p.role}</p>
    <p class="desc">{p.desc}</p>
  </div>
</article>
```
  `<Image>` `sizes` must be set (house convention). If a photo exports in color, add `filter: grayscale(1)` to `.photo` and report. Use `&amp;` for `&` in "Chairman & Managing", "DIRECTOR & CHIEF EXECUTIVE OFFICER", "Corporate Strategy & Business Development", "Petroleum & Natural Gas".
  Grid: 3 columns row 1, 2 columns row 2 (use `grid-template-columns: repeat(3, minmax(0,1fr))` with the 4th/5th items flowing into row 2; match the Figma alignment — row-2 cards align under columns 1 and 2 per node x-positions 350/971).
  Responsive: <1024 → 2 columns; <680 → 1 column (photo stays left, or stacks photo-top on very narrow — keep readable). Nothing removed.

- [ ] **Step 3: Verify** — `bunx astro check` 0/0.
- [ ] **Step 4: Commit** — `git commit -m "feat: add team board of directors section"`

---

### Task 4: AdvisoryBoard section

**Files:** Create `src/components/team/AdvisoryBoard.astro`

**Interfaces:** Consumes tokens, `.container`, `<Image>` (4 advisor photos), `spark-orange.svg`. Produces `AdvisoryBoard.astro` (no props).

- [ ] **Step 1: Fetch context** — `get_design_context` + `get_screenshot` on `1:12977` + `1:12592` (maxDimension 2048). Binding facts: eyebrow `GUIDANCE` (`--color-orange-bright`); H2 `Advisory Board` (Faculty 65px); subheading (Sora muted centered); 4 cards **photo-top (square grayscale) + name + description, centered**, one row; `--color-orange-bright` spark centered below.

- [ ] **Step 2: Build.** Import 4 photos; data array + map. Copy verbatim (name / description):
  1. `Dr. Abhay Karandikar` / `Member, NITI Aayog. Former Secretary, Department of Science & Technology, Government of India`
  2. `Dr. Madhukar Garg` / `Former President, R&D, Refining and Petrochemicals, Reliance Industries Ltd.`
  3. `Shri Neelkanth Mishra` / `Chief Economist, Axis Bank; Head of Global Research and Board member, Axis Capital`
  4. `Dr. Pawan Goenka` / `Chairperson, IN-SPACe, Department of Space, Government of India`
  Use `&amp;` for `&` in "Science & Technology". `<Image widths sizes>` set; grayscale filter if needed. 4-column grid, centered text. Responsive: <1024 → 2×2; <560 → 1 column. `#ff7400` spark centered below.

- [ ] **Step 3: Verify** — `bunx astro check` 0/0.
- [ ] **Step 4: Commit** — `git commit -m "feat: add team advisory board section"`

---

### Task 5: OperatingTeam section

**Files:** Create `src/components/team/OperatingTeam.astro`

**Interfaces:** Consumes tokens, `.container`, `spark-orange.svg`. Produces `OperatingTeam.astro` (no props).

- [ ] **Step 1: Fetch context** — `get_design_context` + `get_screenshot` on `1:11944` (maxDimension 2048) + header `1:13348`/`1:13349`. Binding facts: eyebrow `OPERATING TEAM` (`--color-orange-bright`, centered); H2 `A lean team built to deliver.` (Faculty 65px centered); 6 cells in a **plus/cross grid** — row 1: 4 cells (cols 1–4); row 2: 2 cells under cols 2 & 3 — thin hairlines between cells with `--color-orange-bright` sparks at grid-line intersections (same family as `src/components/about/Scale.astro` dividers — READ it for the hairline+spark technique). Each cell: title Faculty ≈30px `#1e1e3e`; dept label Faculty ≈15px `#949494` uppercase tracked; description Sora.

- [ ] **Step 2: Build.** Data array + map (title / dept / desc):
  1. `Chief Executive Officer` / `REPORTS TO THE STATUTORY BOARD` / `Sets platform strategy, owns alignment with the energy majors and is accountable to the Board for delivery.`
  2. `Chief Incubation Officer` / `PROGRAMMES, GRANTS AND COHORTS` / `Runs incubation end to end, from sourcing and selection through mentorship and venture readiness.`
  3. `Manager, Programmes` / `COHORT DELIVERY` / `Designs the cohort journey and matches ventures to live energy major challenge briefs and pilots.`
  4. `Manager, Partnerships` / `ENERGY MAJORS AND NODES` / `Builds and manages the network of energy majors, IITs and research labs that host and mentor ventures.`
  5. `Manager, Grants` / `NON-DILUTIVE FUNDING` / `Administers grant flows and milestone-linked disbursement to incubated ventures.`
  6. `Finance Manager` / `CONTROLS AND COMPLIANCE` / `Owns financial controls, Section 8 compliance and fund administration across the platform.`
  Layout: 4-column grid; items 1–4 in row 1; item 5 in row 2 col 2, item 6 in row 2 col 3 (use `grid-column-start` to place them). Vertical hairlines between columns + a horizontal hairline between rows, with `--color-orange-bright` sparks at the cross intersections (mirror Scale.astro's spark+border approach). Responsive: <1024 → 2×2 (then the 6 flow 2-per-row); <640 → single column; hairlines/sparks drop when stacked.

- [ ] **Step 3: Verify** — `bunx astro check` 0/0.
- [ ] **Step 4: Commit** — `git commit -m "feat: add team operating team section"`

---

### Task 6: TeamCta section

**Files:** Create `src/components/team/TeamCta.astro`

**Interfaces:** Consumes tokens, `.container`, `PillButton` (from `../PillButton.astro`), `spark-orange.svg`. Produces `TeamCta.astro` (no props).

- [ ] **Step 1: Fetch context** — `get_design_context` + `get_screenshot` on `1:12481` (maxDimension 1600). Binding facts: H2 `Want to build with this team?` (Faculty 65px `--color-ink-alt`, centered); paragraph (Sora muted centered, size from bbox); two **filled** pills; `--color-orange-bright` sparks per design.

- [ ] **Step 2: Build.** Copy verbatim:
  - Paragraph: `We're hiring operators and onboarding mentors as Cohort 1 spins up.`
  - `<PillButton label="Get in touch" href="/contact" variant="filled" />`
  - `<PillButton label="Explore initiatives" href="/initiatives" variant="filled" />`
  Import `PillButton from "../PillButton.astro"`. Centered layout; pills side by side, stack <560. Sparks decorative `aria-hidden`.

- [ ] **Step 3: Verify** — `bunx astro check` 0/0.
- [ ] **Step 4: Commit** — `git commit -m "feat: add team cta section"`

---

### Task 7: Assemble team page + fidelity verification

**Files:** Modify `src/pages/team.astro` (replace `<StubPage>` entirely)

- [ ] **Step 1: Replace `src/pages/team.astro`**
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import TeamHero from "../components/team/TeamHero.astro";
import BoardOfDirectors from "../components/team/BoardOfDirectors.astro";
import AdvisoryBoard from "../components/team/AdvisoryBoard.astro";
import OperatingTeam from "../components/team/OperatingTeam.astro";
import TeamCta from "../components/team/TeamCta.astro";
---

<BaseLayout title="Team · MC²+">
  <TeamHero />
  <BoardOfDirectors />
  <AdvisoryBoard />
  <OperatingTeam />
  <TeamCta />
</BaseLayout>
```

- [ ] **Step 2: Copy-fidelity sweep**
```bash
bun run build
for s in "Leadership" "built for" "accountability" "national gravitas" "GOVERNANCE" "Board of Directors" "Dr. Neeraj Mittal" "Shri Vikas Kaushal" "Petronet LNG" "GUIDANCE" "Advisory Board" "Dr. Abhay Karandikar" "Neelkanth Mishra" "Pawan Goenka" "OPERATING TEAM" "A lean team built to deliver" "Chief Incubation Officer" "NON-DILUTIVE FUNDING" "Finance Manager" "Want to build with this team" "Cohort 1 spins up" "Get in touch" "Explore initiatives"; do
  printf "%s: %s\n" "$s" "$(grep -c "$s" dist/team/index.html)"
done
```
Expected: build exit 0 (7 pages); every string count ≥ 1.

- [ ] **Step 3: Route + chrome regression** — dev server up (kill PID when done): all 7 routes 200; on `/team`, `curl -s localhost:4321/team | grep -o '<a [^>]*aria-current="page"[^>]*>[^<]*Team' | wc -l` is `1`; `grep -c 'id="site-header"'`=1 and `grep -c '<footer'`=1.

- [ ] **Step 4: Full-page visual reference** — `get_screenshot` nodeId `1:11940`, maxDimension 4096; curl to `.superpowers/sdd/figma-team-full.png`. Report path.

- [ ] **Step 5: Commit** — `git add src/pages/team.astro && git commit -m "feat: assemble team page from section components"`

---

## Out of Scope (later plans)
- `/initiatives` (another agent), `/support`, `/portfolio`, `/contact`.
- Scroll animations/motion; contrast remediation (deferred); deploy.
